package SA.irms.reporting.application.view;

public enum ReportValueType {
    TEXT,
    INTEGER,
    DECIMAL,
    MONEY,
    PERCENT,
    DATE,
    DATETIME,
    DURATION_MINUTES,
    BOOLEAN,
    JSON
}
